import React from 'react'
import { task } from './home'
import { View } from 'react-native'


const TaskComponent = ({id, content, status, title}: task) => {
  return (
    <View>

    </View>
  )
}

export default TaskComponent